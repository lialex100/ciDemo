language: go
matrix:
  include:
    - os: linux
      dist: trusty
    - os: osx
      osx_image: xcode7.2
      
go:
  - 1.8
  #- master

branches:
  only:
  - dev


script: go build

after_script: go test ./... -v -cover